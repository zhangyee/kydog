/**
 * 机构账号（CARSI 自动登录）这一版不开放。CARSI 的真机验证与更多学校的接入放到后续版本，
 * 到时把它改回 `true`。
 *
 * 关着的时候两处跟着变，**只认这一个开关**：
 *  - 设置 → 文献检索密钥里「机构账号」那一块不显示（学校选择、账号、密码）；
 *  - `browser_login` 在没配机构时，说明与报错都不再把用户支到设置里去（那里已经没有这一项），
 *    改说 `INSTITUTION_CLOSED_NOTE`。
 *
 * 它只管入口与措辞，不管判据：开关之前已经存下的机构账号照常能用，`loginFlow` 每次执行
 * 仍然只读设置里那一份。
 */
export const INSTITUTION_LOGIN_OPEN = false;

/** 开关关着、又没配机构时，`browser_login` 的说明与报错共用的这一句。 */
export const INSTITUTION_CLOSED_NOTE =
  '这一版**没有开放机构账号登录**，调这个工具只会失败。需要登录的页面交给用户：'
  + '请他自己在这个内置浏览器里登录，或者换一条访问路径（校内网 / VPN）。';
