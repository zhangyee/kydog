type OnboardingWizardProps = {
  mode: 'fresh' | 'recovery';
  corruptNotice: boolean;
};

// 占位实现：Task 11 将填充真正的向导 UI。
export function OnboardingWizard(_props: OnboardingWizardProps) {
  return null;
}
